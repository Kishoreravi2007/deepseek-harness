# Cordis patch overlay for cloud deployment in container environments (Scalix)
- id: webserver
  config:
    host: '0.0.0.0'
    port: 3080
- id: web-runtime
  config:
    trustedHosts:
      - 'dsh-web.run.scalix.world'
      - '0.0.0.0'
